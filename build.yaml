targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            yymusic: lib/data/database/app_database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
