# Phase 6H8 — 大歌单分组浏览计划

2026-09-08，仓库 `Z-YO-YI/YYMusic`，分支 `codex/playlist-window-navigation`，
基于拉取后干净的 `c142daf186efd6c1a4bb717ce02c986066e5ffd9`。

目标：解除自定义歌单只能浏览前200条的限制，不增加一次查询/驻留的条目上限。
已读取主指令§0–44、原始App.tsx/HTML相关歌单映射、H4–H7报告和内容读写/路由实现；
五份指纹、44图标、52确定性审计产物及ZIP24项逐字节复核通过。

- 先记录ADR-064。继续使用Repository既有任意offset的单SQL快照，不修改Schema或合同。
- 首组仍按20条扩展到200条，之后明确切换下一组；上一组读取完整200条。
  每组替换当前快照，不拼接不同时刻的排序页，不复制曲目或额外缓存整份歌单。
- 翻页必须来自当前快照、活动页面且无进行中操作；过期回调、双击、覆盖路由不产生新查询。
  读取失败保留旧内容但禁用操作，显式刷新重试目标组；刷新/失效通知保持当前组与查询上限。
- 如果删除使当前组越界，按最新总数回到最后有效组再读取，不显示假空歌单；整个歌单被删除仍为Missing。
- Phone/Tablet/Windows复用YYButton和现有Token显示范围/上一组/下一组。
  页面仅在实际组偏移变化后回到顶部，刷新和跨断点保持滚动与根播放状态；大列表继续SliverList.builder。
- 不根据未读取邻居猜测移动锚点；组边缘移动仍按当前可信快照禁用。
- 新增有界分页、并发/取消、缩减恢复、真实SQLite及三端Widget/Golden测试，保留全部旧基线。
  格式、严格分析、完整Flutter/Node、生成/指纹/许可检查后构建Android并提交推送stacked Draft PR。
  核对精确提交的GitHub源码/Android/Windows结果；不合并、不发布、不提交日志/安装包/秘密。

本批不包含播放全部/随机、系统歌单、导入、全屏播放器、平台媒体或安装发行。
默认新安装仍空库；Windows Debug不是通用安装程序；网页对照限制和实机性能验收不能用Golden替代。
