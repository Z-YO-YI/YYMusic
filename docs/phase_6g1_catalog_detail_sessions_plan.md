# Phase 6G1 — 专辑／艺人详情会话

2026-09-06，基于 fetch/pull 后的 `b72413e`，独立分支 `codex/catalog-detail-sessions`。
前置 Phase6F 的 push 33984812889 与 PR 33984831085 均成功；PR 的源检查、Android、
Windows构建、61张Golden及真实窗口步骤已核实，专用音频运行跳过，不作为新音频验收。
五份设计源指纹一致；Phase0完整审计保持有效，本轮重读完整App.tsx和开发总指令。

## 本阶段目标与来源

依据主指令13/17.3/36的Library→Albums→Artists顺序，先实现可独立验证的详情状态层，
下一增量接路由和三端界面；不把新增控制器称为详情页面已交付。
基础HTML的专辑卡、艺人及曲目结构是入口参考，原型的media-select不等于真实专辑详情。
App.tsx的NEW_ICON_SPRITE/POLISH_CSS继续作为下一批UI的最终图标、20圆角、字重和阴影依据。

- AlbumRef/ArtistRef是闭合的强类型目标；摘要缺失与错误分开，禁止同名/跨来源兜底。
- 专辑摘要+曲目，艺人摘要+专辑+曲目；分区独立加载/失败/重试，分页20、200原始行上限。
- 不读取整库；完整身份去重，offset按原始行递增，保留失效引用与真实聚合计数。
- 刷新取消全部旧结果；各分页独立取消，慢旧请求不可回写新状态。
- 根会话注册表跟踪每次打开的控制器；关闭页面取消结果并排空实际Future，根存储最后关闭。
- 本批只读，不新增播放器、数据库、Schema、依赖或音乐源请求，也不提前显示假详情按钮。

## 文件与风险

新增features/catalog_detail/common下状态、控制器及会话注册表；修改app/dependency_graph.dart、
架构门禁和README/状态/测试矩阵/ADR；新增独立Fake与真实SQLite/关闭顺序测试。
主要风险：同ID不同来源串数据、关联查询越界、迟到结果、重复分页、根关闭/单页关闭竞态。
会话未被界面使用前不改变现有61张Golden或任何页面行为；下一批才能验收三端详情交互。

## 出口条件

格式、严格分析、全量Flutter/Node、设计ZIP与许可证门禁、生成代码稳定及Android本机构建；
审查后独立提交推送，stacked Draft PR，精确提交GitHub Android/Windows分别核验。
不自动合并、发布，不把只读状态合同或Debug构建称为上线。

## 审查发现的同类边界修正

短中间页（例如15条且hasMore=true）会使固定20条末页越过200上限；新增测试在详情/搜索复现215条，
音乐库末页请求也复现应为5但实际20。将本批范围补充为三个投影的末页剩余额度修正及三项回归，
不改变排序、数据库、UI或既有61张截图；保留原有分页门禁并增强为校验剩余额度和实际截断。
