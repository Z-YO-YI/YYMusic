# Phase 7H1A：根睡眠截止定时核心

2026-09-13，基线2fb29ec，fetch/ff-only pull且干净；分支codex/sleep-deadline-core，Draft base codex/phase7-exit-audit。先ADR097，再实现和验证。前置审计CI仍运行。

本批只支持关闭及15/30/60分钟截止定时，按根UTC时钟判断剩余时间，注入单次Timer工厂供确定性测试；不逐秒累加、不修改播放状态模型或数据库，不跨进程恢复。根拥有意图/Timer/关闭清理，UI后续只读投影；到期经过根命令队列暂停，取消/重设/关闭前尚未开始的旧操作撤销，已进入引擎的pause排空、不自动重播。到期取消尚未执行的自动下一首意图；本曲结束模式单列下一增量，必须先处理准确entry与完成事件拦截，不冒称已实现。

测试覆盖三个时长、取消/重设/旧回调、提前/延迟唤醒、一次性执行、暂停/空库、正在加载/切歌、到期与自动推进竞争、通知重入、暂停错误、根关闭前后排空。全量Flutter/Node/格式/严格分析/生成迁移/Android预检通过后提交push/Draft；无UI/Golden修改，无设备安装或发行验收。
