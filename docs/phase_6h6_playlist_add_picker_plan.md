# Phase 6H6 — 添加到歌单原生选择器计划

2026-09-08，分支`codex/playlist-add-picker`，基于fetch/pull后的
`4847f29b66d1b3d82f32e71aa43309cc3f6dfac4`；PR #50 OPEN Draft。
五份指纹与ZIP24项再次通过，延续Phase0对App.tsx/基础HTML的完整审计。

1. 先记录ADR-062；为选择器增加显式、有界的自定义歌单元数据读取，而非订阅整库列表。
   只读一条SQL、名称字面子串筛选（ASCII大小写折叠）、更新时间降序及稳定ID排序，limit+1判断更多。
2. 根注册惰性选择会话，先订阅已有失效通知再读取；20→200一致前缀、明确上限及筛选缩小范围。
   错误/过期数据不可选择；请求代次隔离，关闭等待实际查询/取消/写入，不将Stream.cancel等同于SQL取消。
3. 音乐库与专辑/艺人详情的歌曲菜单增加“添加到歌单”；根原生模态路由只携带TrackRef与标题，
   不将路径/媒体URI写到URL、日志或新存储。覆盖整个Shell、取消迟到播放、返回恢复原行焦点。
4. Phone使用YYBottomSheet，Tablet/Windows使用YYDialog，复用已审计SVG/Token。
   原生名称筛选输入支持IME；显式筛选，输入变更时旧列表不可选择。加载/空/错误/重试/成功反馈完整。
5. 点“添加”借用唯一根PlaylistController.addTrack；重复引用允许成为独立条目，失效来源引用可保留。
   稳定ID、当前快照和请求身份校验；不新建播放器、不复制文件、不覆盖整份歌单、不自动重试写入。
6. 覆盖三端、键鼠/返回/焦点闭环、130%文字/键盘/零尺寸/旋转、旧回调、真实SQLite、根关闭和离页失败。
   新增/变更Golden逐张检查；格式/分析/完整Flutter与Node、Drift/Schema/lock/指纹/许可、Android预检，
   提交推送stacked Draft PR并核验精确提交GitHub双平台运行。

本批仅添加到已有歌单。选择器内创建并添加的原子组合、播放全部/随机、系统歌单和大歌单完整浏览仍分批实现。
空选择器提示先到音乐库创建歌单，不做“创建成功但添加失败”的伪原子行为。
figma-design-to-code在线节点流程因离线ZIP无fileKey/nodeId不适用，采用其组件/原始资产复用原则；
不虚构在线设计读取、不绕过既有网页对照安全限制；无新依赖/Schema、导入、网络、发行或全局环境修改。
