# Phase 6H5 — 原生歌单内容管理计划

2026-09-08，基于 fetch/pull 后的 `97465063ba8b48195ed3e117169c84c0af6ab48a`，
分支 `codex/playlist-content-surfaces`。前置 PR #49 OPEN Draft，push34203551280/PR34203648385成功。
五份指纹、ZIP24项逐字节再次通过，延续Phase0完整App.tsx/基础HTML审计，不重新解压或修改原件。

1. 先记录ADR-061，再扩展原生歌单路由、会话动作与根安全反馈合同。
2. 音乐库自定义歌单增加查看入口；路由只携带稳定歌单ID，保留音乐库返回状态。
3. Phone单列、Tablet横屏主从/竖屏组合、Windows独立宽布局；复用YY组件及44个审计SVG。
   使用基础HTML的歌单→歌曲交互、App.tsx最终图标/20px菜单与14px歌单图标规则，原生补充管理动作。
4. 复用H4一致内容窗口，Loading/Empty/Missing/Error/重试/更多完整；保留重复条目与不可用引用。
   本批仍为20→200前缀，明确上限，不以其冒充完整大列表、播放全部或系统歌单视图。
5. 单首播放调用唯一根播放器；移除与上下移动调用唯一根歌单命令，不在UI写库、不覆盖整表。
   移动使用当前可见条目身份和锚点，不跨未知窗口边界；过期快照/菜单回调不可执行。
6. 路由覆盖/返回/刷新撤销未执行播放意图；已接受写入登记排空，不因离页丢弃。
   保存失败留在安全根反馈中，返回音乐库仍可见；不自动重试不确定写入。
7. 页面状态高于AdaptiveRoot，手机/平板切换保持读取会话和滚动；菜单覆盖整页、Back/Esc关闭、焦点恢复。
8. 新增动作、路由、Widget及三端Golden；逐张查看新增/变更图片。完整格式/分析/Flutter/Node、
   生成/Schema/lockfile/参考/许可校验、本地Android预检后提交推送stacked Draft PR并核验精确云端双平台。

figma-design-to-code技能的在线节点流程不适用于本次离线ZIP（无fileKey/nodeId）；
采用其复用组件和原始资产原则，以用户指定的离线导出包为依据，不虚构在线截图或节点。
既有网页对照安全限制不绕过；本批不新增依赖、Schema、来源/导入、拖拽排序、添加选择器、播放全部/随机、发布。
