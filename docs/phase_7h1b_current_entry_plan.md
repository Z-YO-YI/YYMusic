# Phase 7H1B：本曲结束睡眠意图

2026-09-13；基线938a896，已fetch/ff-only pull，干净分支codex/sleep-current-entry，Draft base codex/sleep-deadline-core。H1A双CI仍运行，当前无失败或未同步提交。

先ADR098，再实现唯一根的本曲结束模式。只允许绑定当前已经加载的准确queue entry，不用TrackRef代替条目身份；同一歌曲重复项必须区分。没有可用的已加载条目（空库、加载中、已完成、错误）拒绝设置并保留原意图，不自动播放或排队等待将来歌曲。

暂停/恢复和当前条目seek保留意图；手动切到另一项、重载同一项、停止、清空或错误撤销，不跟随下一首。重复单曲/列表及随机模式不能越过睡眠意图。完成事件在根创建自动推进之前一次性消费为expired，音频已经完成，不补发pause/stop/seek或写队列。取消或改分钟定时互斥，旧分钟回调无效；通知重入不得补发已消费完成事件。

不增加Widget、平台API、存储或依赖。本批验证准确条目/重复/重复模式/暂停恢复/seek/切歌/取消/重设/失败/关闭/完成通知重入及与H1A共存，完整回归和双平台GitHub构建仍按精确提交验收。原生设置界面另列H2。
