# Phase 7H4C2a：生产根音量意图隔离

## 范围与真实进度

接续H4C1淡出契约，先完成根音量真值这一独立可验证前置条件。**本批不启用淡出采样，不改变睡眠到期的直接暂停行为**；H4C2b仍须实现逐步写入、用户取消及音量恢复。Phase7和Phase8–11未完成，不等于实机或上线验收。

开始前确认仓库Z-YO-YI/YYMusic、干净工作区，fetch/ff-only pull已同步。基线49a213f，分支codex/playback-volume-intent，Draft base codex/sleep-fade-envelope。父push34736938583/PR34736953793核验时queued，不能宣称通过。

## 先失败复现，再修复

新增3项真实PlaybackController测试先运行全部失败：无事件回报的成功命令不更新音量，加载状态与未加载状态的迟到旧快照都覆盖新用户音量。原失败日志为本地build/phase7h4c2a_before.log，构建日志不入库。

根新增可空_userVolume：尚未接受有效音量请求前，沿用引擎初始音量；通过引擎可用性和参数校验后、调用引擎前锁定上次确认值，防止同步/迟到回报泄漏。串行命令成功且根未关闭才确认新值并发布，不依赖插件另发volume事件。失败保留原确认值，仍通过原_guarded安全错误路径返回；不是假成功，也不声称失败的插件已回滚硬件。

引擎状态的普通和未加载两条音量投影均优先使用_userVolume。其他播放状态、进度、速率保持原有处理。停止/切曲不丢失应用音量意图。PlaybackState.volume现明确为应用已确认音量意图，不是系统输出音量，也不是将来的临时淡出振幅。

所有应用音量仍通过唯一根串行队列写入，无第二播放器、UI Timer或新外部接口。关闭仍排空在途调用，但不发布迟到确认，不关闭借用引擎；排队未执行命令按既有策略拒绝。

## 验证

23项新增单测：3项原问题回归、初始化来源、三档音量、慢写保护、同步回报、失败安全提示、首次失败、重试不播放、四种非法值、FIFO/失败后继续、通知重入、停止/重载与其他状态、关闭排空/拒绝队列/引擎重入关闭。

完整2137 Flutter通过（154秒），158 Node通过（48秒）；566 Dart文件格式零修改，严格分析0问题（21.3秒）。217旧Golden未改，未修改依赖、代码生成输入或schema，因此未重复生成迁移。diff及变更文件秘密扫描通过。

Android Debug预检22.3秒；48打包资产、完整音频许可及v2单签名者通过。APK232294048 bytes，SHA256为7f1a15f6fcdbdf3fea686d0128a6d207c3abdd0f859dfc6ae455b5ebf22adf69。保留Java原生访问警告，无新本地Windows构建或设备听感验证。父双CI再次核验仍queued；本提交云端按新SHA独立检查，不拿本地结果冒充GitHub通过。APK和日志不提交源码仓库。

参照指纹：24解压文件与原ZIP逐字节一致；开发总指令SHA256为5f024c778be878afc6fcdcc2d3051b1aec5d3357b2fd01d2ea23b1a71066cfcf。沿用Phase0完整App.tsx（含NEW_ICON_SPRITE/POLISH_CSS）与HTML审计，无新视觉解释或WebView。

## 下一步H4C2b

首次淡出即使用户未调过音量，也必须先锁定根意图；临时增益不得走公开setVolume而把衰减值记成用户值。每次采样独立串行，等待置于队列外，使用单调经过时间。取消/新定时/暂停/切曲/关闭撤销旧采样；已在途写完成后恢复最新确认音量，错误可观察，不自动播放。上述后续路径尚未由本批23项测试验证，不借用纯曲线测试宣称已完成。
