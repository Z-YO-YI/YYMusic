# Phase 6H9 — 歌单播放全部与随机播放计划

2026-09-08，仓库Z-YO-YI/YYMusic，分支codex/playlist-play-all，
基于pull后干净的33187c0491a6e6850f5a5bc6986df61f24e0c075。
前置PR #53为OPEN Draft，精确提交两组源码/Android/Windows全部成功。
再次校验五份指纹、44图标/52确定产物和ZIP24项；沿用Phase0完整App.tsx/HTML审计，
已重读主指令歌单/队列/软引用/阶段要求及根播放、Repository和页面实现。

- 先记录ADR-065。Repository新增一次性轻量播放计划：同一SQL读取父身份和全部条目的完整来源引用、顺序与可用性。
  不受200条浏览窗口限制，不读取整曲Metadata/Artwork/艺人，不N+1，不拼接可能改变的分页，不写曲库。
- 共用模型保留重复条目、位置、不可用和缺失引用。系统歌单拒绝；Missing和现有空歌单区分。
- 根播放器新增单个串行批量命令：冻结引用、生成唯一队列ID、预先校验随机顺序，再提交完整新队列与随机模式。
  使用现有原子saveQueue与根播放解析/引擎；不串联多个独立UI命令，不新增播放器/存储。
  非空成功读取后才替换队列；空/全部不可用不修改当前播放；提交前撤销不写，已提交不做补偿回滚。
- 页面显示“播放全部 / 随机播放”及替换当前队列说明。完整计划过滤已标记不可用项，显示跳过数，保留歌单引用。
  新读/根操作均先登记排空；忙碌、当前快照、菜单、覆盖路由与撤销代次阻断旧回调，离页不让迟到加载开始音频。
- 复用YYButton、原始play/shuffle SVG及现有Token，Phone/Tablet/Windows用Wrap适配130%文字。
- 补模型/真实SQLite/Fake、根序列与随机、会话取消/关闭、三端UI/Golden测试。
  格式/严格分析/全量Flutter/Node、生成与Schema/lock/指纹/许可门禁，Android本地预检后提交推送stacked Draft PR。
  核验精确提交GitHub双组Android/Windows，不合并/发布、不提交凭据/用户数据/构建产物。

本批不完成系统歌单、全屏队列、导入、REST、后台媒体、发行或大库实机Profile。
测试发现Windows顶层WindowFrame在零尺寸直接丢弃Navigator，而非隐藏；
本批同时修复为保留子树、暂停Ticker的离屏布局，使用上一次有效窗口约束，
补原生窗口框架和歌单恢复回归，避免最小化丢失路由/会话或让延迟播放触发。
读取后新出现的文件/来源失效仍由既有播放错误流程反馈；不宣称所有运行时错误均可自动跳过。
网页截图限制不绕过，离线ZIP没有在线Figma节点，figma-design-to-code沿用组件/资产复用，不虚构在线读取。
