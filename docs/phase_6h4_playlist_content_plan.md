# Phase 6H4 — 歌单内容读取会话计划

2026-09-08，`Z-YO-YI/YYMusic`，`codex/playlist-content-sessions`，
基于 fetch/pull 后的 `cbe7bdc1b8c3e11bde293250db82d48162e9693c`。
前置 push 34198822942 / PR 34198919253 全部成功，Draft PR #48 仍 OPEN。
五份指纹、ZIP 24 项逐字节复核通过；原始 App.tsx 与基础 HTML 审计依据不变。

- 先记录 ADR-060，再扩展 CollectionRepository 的只读内容快照与失效通知合同。
- 单条 SQL 同时读取歌单、总条目数、指定窗口及曲目/艺人；先限制条目窗口，再展开艺人。
  按条目 ID 聚合，保留重复曲目；完整三字段来源引用左连接，未解析曲目保留空 Track，不伪造曲库记录。
- 根注册只读会话，构造零查询/订阅；订阅失效通知后按需读取，增删/排序/元数据/曲目变化刷新完整已加载窗口。
  初始 20、最多 200 的可见前缀，底层支持任意 PageRequest offset；达到窗口上限明确 capped，不宣称全部加载。
- 不拼接不同版本的排序页，不以更新时间当版本号；同毫秒改序也由失效通知重新读取。
  通知可能保守涵盖事务回滚，不是提交日志；必须读取当前持久内容，不能当作写入成功。
  通知合并，旧响应不可覆盖新请求；Loading/Empty/Missing/Error、保留旧数据但标记过期、明确重试。
- 查询 Future 与订阅建立/取消全部登记，根关闭拒绝新会话并排空后再关共享数据库；不依赖 Stream.cancel 终止 SQLite。
- 不新增 UI/路由、播放实例、网络请求、Schema、依赖或第二份曲库；本批只交付下一阶段界面需要的读取基础。
  系统歌单的实时收藏/历史/队列投影另行实现，不把持久条目当作系统视图。
- 补模型、真实 SQLite、通知/并发/关闭测试与边界审计；完整格式/分析/Flutter/Node、生成/许可/指纹零差异，
  Android 本地预检后提交推送 stacked Draft PR，精确核验 GitHub Android/Windows；不合并、不发布。

网页截图对照仍受既有安全限制，不绕过；Golden 和构建不能替代网页对照或实机音频验收。
